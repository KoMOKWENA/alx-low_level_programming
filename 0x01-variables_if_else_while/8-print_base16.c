#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9 and letters between a to f.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int f;

for (f = 48; f < 58; f++)
{
putchar(i);
}
for (f = 97; f < 103; f++)
{
putchar(f);
}
putchar('\n');
return (0);
}
